# ChroLens Mimic Web

這是 ChroLens Mimic 的 Next.js 網站版本，將原本的靜態 HTML 網站轉換為現代化的 React 應用。

## 特色

- ✨ **Next.js 14** - 使用最新的 Next.js App Router
- 🎨 **深色/淺色主題** - 支援主題切換並持久化儲存
- 📱 **響應式設計** - 適配各種螢幕尺寸
- 🚀 **靜態導出** - 可部署到 GitHub Pages
- 🎯 **TypeScript** - 完整的類型安全
- 📊 **Microsoft Clarity** - 整合分析追蹤

## 開始使用

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

然後在瀏覽器開啟 [http://localhost:3000](http://localhost:3000)

### 建置與導出

```bash
npm run build
```

建置完成後，靜態檔案會在 `out/` 資料夾中。

### 本地預覽建置結果

```bash
npx serve out
```

## 專案結構

```
web/
├── src/
│   ├── app/                # 頁面路由
│   │   ├── layout.tsx      # 根佈局
│   │   ├── page.tsx        # 首頁
│   │   ├── script-editor/  # 腳本編輯器頁面
│   │   └── changelog/      # 更新日誌頁面
│   ├── components/         # React 組件
│   │   ├── Sidebar.tsx
│   │   ├── ThemeToggle.tsx
│   │   └── Pagination.tsx
│   ├── hooks/              # 自定義 Hooks
│   │   └── useTheme.ts
│   └── data/               # 資料檔案
│       └── versions.ts
├── public/                 # 靜態資源
├── next.config.js          # Next.js 配置
├── tsconfig.json          # TypeScript 配置
└── package.json           # 專案依賴

```

## 部署到 GitHub Pages

1. 確保 `next.config.js` 中的 `basePath` 設定正確
2. 執行 `npm run build` 建置專案
3. 將 `out/` 資料夾的內容推送到 GitHub
4. 在 GitHub 儲存庫設定中啟用 GitHub Pages，選擇部署來源

## 主要功能

### 頁面導航

- **基本介紹** (`/`) - ChroLens Mimic 的功能介紹
- **腳本編輯器** (`/script-editor`) - 完整的指令參考文檔
- **更新日誌** (`/changelog`) - 版本更新歷史（支援分頁）

### 主題切換

使用左側邊欄底部的切換按鈕在深色和淺色主題之間切換。主題偏好會儲存在瀏覽器的 localStorage 中。

### 分析追蹤

網站整合了 Microsoft Clarity 進行用戶行為分析，追蹤 ID 在 `layout.tsx` 中配置。

## 技術棧

- **Framework**: Next.js 14
- **Language**: TypeScript
- **Styling**: CSS Modules
- **Deployment**: 靜態導出 (Static Export)

## 授權

與 ChroLens Mimic 主專案相同的授權條款
